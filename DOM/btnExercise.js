/*let btn = document.querySelector("#btn");
let size = window.innerWidth;
document.querySelector(".screen-size").innerHTML = size;
const btnIncrement = (e) => {
    e.preventDefault();
    e.target.innerHTML = parseInt(e.target.textContent) + 1;

}
btn.addEventListener("click", btnIncrement);
let frm = document.forms[0];
frm.style.background = "silver";
let txt_search = frm.querySelector('input[type="text"]');
frm.addEventListener("submit", function(e) {
    e.preventDefault();
    console.log(txt_search.value);
});*/
// js de btn.html